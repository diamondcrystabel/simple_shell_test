#include "shell.h"

/**
 * _strlen - counts a string
 *
 * @str: string to be counted
 * Return: lenght of str
 */

size_t _strlen(char *str)
{
	size_t i;

	for (i = 0; str[i]; i++)
	{
	}
	return (i);
}
